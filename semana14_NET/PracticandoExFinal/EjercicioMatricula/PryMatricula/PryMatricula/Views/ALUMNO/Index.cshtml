@model IEnumerable<PryMatricula.Models.ALUMNO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CODCUR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.APENOM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAGO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CODCUR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.APENOM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAGO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NROMAT }) |
            @Html.ActionLink("Details", "Details", new { id=item.NROMAT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NROMAT })
        </td>
    </tr>
}

</table>
